[package]
name = "tracker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
cdylib = []
default = ["cdylib"]

[dependencies]
cerulion_core = { workspace = true }
native_ros2_messages = { workspace = true }

# Consumes the DetectionArray, so it codegens the same type from the shared yaml.
[build-dependencies]
cerulion_core = { workspace = true }
serde_yaml = { workspace = true }
