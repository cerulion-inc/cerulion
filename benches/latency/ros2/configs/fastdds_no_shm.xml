<?xml version="1.0" encoding="UTF-8" ?>
<!--
  FastDDS profile — UDPv4 only, SHM explicitly disabled.

  FastDDS's built-in transports include SHM on Humble defaults; this
  profile registers a UDPv4 transport descriptor and turns off the
  built-in transports so SHM is genuinely absent rather than just
  "preferred away" by routing.
-->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <profiles>
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>udpv4_transport</transport_id>
        <type>UDPv4</type>
      </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="participant_default" is_default_profile="true">
      <rtps>
        <userTransports>
          <transport_id>udpv4_transport</transport_id>
        </userTransports>
        <useBuiltinTransports>false</useBuiltinTransports>
      </rtps>
    </participant>
  </profiles>
</dds>
