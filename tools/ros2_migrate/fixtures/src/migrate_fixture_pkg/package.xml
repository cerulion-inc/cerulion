<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>migrate_fixture_pkg</name>
  <version>0.1.0</version>
  <description>
    Hand-written fixture sources for the cerulion ros2 migrate accept/reject
    matrix. Every translation unit compiles; the safe_* files carry exactly
    one provable rewrite site each, the unsafe_* files carry exactly the
    refusal class their name says.
  </description>
  <maintainer email="dev@cerulion.com">Cerulion</maintainer>
  <license>AGPL-3.0-only</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <depend>rclcpp</depend>
  <depend>std_msgs</depend>
  <!-- geometry_msgs/Twist is a PLAIN fixed type, so Fast DDS
       actually loans it — the loaning path std_msgs/String never reaches. -->
  <depend>geometry_msgs</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
